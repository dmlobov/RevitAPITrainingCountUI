<Window x:Class="RevitAPITrainingCountUI.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RevitAPITrainingCountUI"
             mc:Ignorable="d" 
             Height="225" Width="250"
             Title="Выберете">
    <StackPanel>
        <Label Content="Узнать количество труб" Margin="5"/>
        <Button Content="Выполнить" Command="{Binding SelectPipe}" Margin="5"/>
        <Label Content="Узнать объем стен"/>
        <Button Content="Выполнить" Command="{Binding SelectWalls}" Margin="5"/>
        <Label Content="Узнать количество дверей" Margin="5"/>
        <Button Content="Выполнить" Command="{Binding SelectDoors}" Margin="5"/>
    </StackPanel>
</Window>
